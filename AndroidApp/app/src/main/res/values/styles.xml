<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/base01</item>
        <item name="colorPrimaryDark">@color/base00</item>
        <item name="colorAccent">@color/base02</item>
        <item name="android:windowBackground">@color/base01</item>
        <item name="android:textColor">@color/base05</item>
    </style>
</resources>
